---
title: "Probiotics"
output: html_document
date: "2025-09-29"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
install.packages("metafor")
install.packages("dplyr")
library(metafor)
library(dplyr)
```

```{r}
dataset <- read.csv("01-raw-data.csv", header=TRUE)
```

```{r}
res <- rma(yi = yi, vi = vi, data = dataset, method = "REML")
summary(res)
```

